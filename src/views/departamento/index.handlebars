<div class="col-lg-12">
            <div class="page-header d-flex justify-content-between justify-content-center">
              <h1 id="tables">Tables</h1>
            <div>
              <a class="btn btn-primary" href="/departamento/create"><i class="fa-sharp fa-solid fa-plus"></i></a>
            </div>
            </div>

            <div class="bs-component">
              <table class="table table-hover">
                <thead>
                  <tr>
                    <th scope="col">Type</th>
                    <th scope="col">Column heading</th>
                    <th scope="col">Column heading</th>
                    <th scope="col">Column heading</th>
                  </tr>
                </thead>
                <tbody>
                  {{#each departamentos}}
                  <tr class="table-dark">
                    <th scope="row">{{name}}</th>
                    <td>{{sigla}}</td>
                    <td></td>
                  
                  </tr>
                  {{/each}}
                </tbody>
              </table>
            </div><!-- /example -->
          </div>
    